<br />

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>What I'm Learning</h1>
  <% current_user.enrolled_courses.each do |course| %>
    <h3 class="text-center">
      <%= link_to course.title, course_path(course) %>
    </h3>
    <br />
    <% if course.image? %>
      <%= image_tag course.image, class: 'img-responsive course_image' %>
    <% else %>
       <%= image_tag "missing_lesson_image.jpg", class: 'img-responsive course_image' %>
    <% end %> 
    <br />
    <div class="col-xs-11 col-sm-7 col-xs-offset-1 col-sm-offset-1">
      <%= course.description %>
    </div>
    <div class="col-xs-12 col-sm-4 text-center">
      <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
    </div>
    <hr class="col-xs-8 col-xs-offset-2">
    <br class="clr" / >
  <% end %>

  <h1>What I'm Teaching</h1>
  <% current_user.courses.each do |course| %>
    <h3 class="text-center">
      <%= link_to course.title, course_path(course) %>
    </h3>
    <br />
    <% if course.image? %>
      <%= image_tag course.image, class: 'img-responsive course_image' %>
    <% else %>
       <%= image_tag "missing_lesson_image.jpg", class: 'img-responsive course_image' %>
    <% end %> 
    <br />
    <div class="col-xs-11 col-sm-7 col-xs-offset-1 col-sm-offset-1 ">
      <%= course.description %>
    </div>
    <div class="col-xs-12 col-sm-4 text-center">
      <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
    </div>
    <hr class="col-xs-8 col-xs-offset-2">
    <br class="clr" / >
  <% end %>
</div>